import { colors } from "./colors";

export const theme = {
  colors,

  radius: {
    sm: 8,
    md: 12,
    lg: 16,
  },

  spacing: {
    xs: 4,
    sm: 8,
    md: 16,
    lg: 24,
    xl: 32,
  },
};